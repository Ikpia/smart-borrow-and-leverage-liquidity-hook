# Shared

Cross-component artifacts:

- `shared/abis/*.json`: canonical contract ABIs generated from Foundry artifacts.
- `shared/types/index.ts`: shared frontend/indexer TypeScript types.

Regenerate ABIs:

```bash
make abis
```
